What happens when you modify the Content-Length?
After modifying the content-length to 5, the client only reads 5 bytes of the body.
With curl, the HTML message doesn't appear, the web page appears white not green without anything in it.